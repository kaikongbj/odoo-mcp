# FastMCP 服务器部署指南

## 快速部署步骤

### 1. 环境准备

确保你的 Odoo 17 环境已安装并运行：

```bash
# 确保 FastMCP 依赖已安装
pip install fastmcp
```

### 2. 模块安装

在 Odoo 中安装 MCP 服务器模块：

```bash
# 将模块文件夹复制到 Odoo addons 目录
# 然后在 Odoo 界面中安装 "MCP Server" 模块
```

### 3. 服务器配置

在 Odoo 中创建新的 MCP 服务器记录：

- **名称**: 给服务器起个名字 (例如: "主 MCP 服务器")
- **服务器 URL**: <http://localhost:10888>
- **服务器类型**: 选择 "fastmcp"
- **状态**: 设置为 "激活"

### 4. 启动服务器

在 Odoo 界面中：

1. 进入 "MCP 服务器" 菜单
2. 找到你创建的服务器记录
3. 点击 "启动服务器" 按钮

### 5. 验证部署

服务器启动后，你应该能看到：

- 服务器状态变为 "激活"
- 日志中显示服务器在端口 10888 启动
- 可以通过 <http://localhost:10888> 访问服务器

## 故障排除

### 常见问题及解决方案

#### 1. 端口被占用错误

```
错误: 端口 10888 已被占用
解决: 
- 检查是否有其他 MCP 服务器在运行
- 使用 netstat -an | find "10888" 查看端口使用情况
- 停止冲突的服务或修改配置使用其他端口
```

#### 2. 数据库连接错误

```
错误: 无法获取Odoo环境
解决:
- 确保在 HTTP 请求上下文中执行操作
- 检查 Odoo 数据库连接是否正常
- 重启 Odoo 服务
```

#### 3. 工具注册失败

```
错误: 工具函数未响应
解决:
- 检查服务器日志中的错误信息
- 确保所有依赖库已正确安装
- 重新启动 MCP 服务器
```

## API 使用示例

### 查询 Odoo 模型

```json
{
  "tool": "query_odoo_model",
  "arguments": {
    "model_name": "res.partner",
    "domain": "[]",
    "fields": "[\"name\", \"email\"]",
    "limit": 10
  }
}
```

### 创建记录

```json
{
  "tool": "create_odoo_record", 
  "arguments": {
    "model_name": "res.partner",
    "values": {
      "name": "新客户",
      "email": "customer@example.com"
    }
  }
}
```

### 获取记录详情

```json
{
  "tool": "get_odoo_record",
  "arguments": {
    "model_name": "res.partner",
    "record_id": 1
  }
}
```

## 配置选项

### 服务器设置

- **端口**: 默认 10888，可在代码中修改
- **主机**: 默认监听所有接口 (0.0.0.0)
- **传输模式**: SSE (Server-Sent Events)

### 性能调优

- **数据库连接**: 使用连接池优化数据库访问
- **日志级别**: 生产环境建议设置为 INFO 或 WARNING
- **超时设置**: 根据需要调整网络和数据库超时

## 监控和维护

### 日志位置

- Odoo 日志：包含 MCP 服务器的启动和错误信息
- 自定义日志：使用 `_logger` 记录的详细操作日志

### 健康检查

定期检查：

- 服务器状态是否为 "激活"
- 端口 10888 是否可访问
- 数据库连接是否正常
- 工具函数是否响应正常

### 维护建议

- 定期重启服务器以清理内存
- 监控数据库连接数量
- 备份 MCP 服务器配置
- 保持依赖库更新

---
**部署难度**: 简单  
**预估时间**: 15-30 分钟  
**技术要求**: Odoo 17 基础知识
